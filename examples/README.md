# Seagull App Examples

A few apps that use the `@seagull/core` and showcase different things you
can do with the framework.

## Preparation

When you have checked out this repository, do a "npm run build" in the top
level project folder, since all projects use the framework directly from disk.
